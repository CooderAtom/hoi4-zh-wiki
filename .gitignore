# ══════════════════════════════════════════════════════════════════
#  绝不上传的内容
# ══════════════════════════════════════════════════════════════════

# 原始抓取缓存（约 1 GB）。三个不能上传的理由：
#   1. 体积超标——GitHub 源仓库建议上限 1 GB，加上它直接 1.6 GB；
#   2. 内容违规——里面保留着本站已按 CC BY-SA 要求删除的商店/论坛/视频等外部入口；
#   3. 含隐私数据——cache/edge-profile/ 是浏览器配置目录，内有 Vpn Tokens、
#      Trust Tokens 以及 Cookie / 登录数据，上传等于泄露。
/cache/

# Windows 快捷方式（内含本机绝对路径，对其他人无意义）
/index.html.lnk

# 编辑器与系统垃圾文件
node_modules/
Thumbs.db
desktop.ini
*.tmp
